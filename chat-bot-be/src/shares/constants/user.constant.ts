export const enum UserRoles {
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest',
}

export const enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
}

export const enum UserGender {
  MALE = 'male',
  FEMALE = 'female',
}

export const enum Userlanguage {
  EN = 'en',
  VI = 'vi',
}

export const enum UserTheme {
  LIGHT = 'light',
  DARK = 'dark',
}

export const enum UserTokenType {
  ACCESS = 'access',
  REFRESH = 'refresh',
}
